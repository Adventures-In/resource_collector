name: resource_collector
description: Making it super easy to collect and organise your learning resources.

publish_to: 'none'

version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_web_compilers:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/images/gmail-logo.png